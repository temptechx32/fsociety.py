# TXSH

TXSH is a Shell that Allows People To Have a diffrent shell its made by Techx

To Run the installer do this or install the Shell script on

<code>
download The install.sh that you see first

Open Terminal
and Go to the Shell file

run:
chmod +x install.sh && ./install.sh

let it run until its done

run:
cd src/TXSH/ && chmod +x install.sh && ./install.sh

let is also finish it Will run the cmd
<code>txsh</code>
if you want to change the name then do this

run:
cd /usr/local/bin/

then:
sudo nano txsh

press:

SHIFT + O
Change name to that what you want
ENTER
SHIFT + X

done
</code>

Commands That work are none you need to wait till i publish the shell script
